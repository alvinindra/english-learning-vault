# Grammar: Simple Present for Docs

Developers use simple present for facts.

**Pattern:** Subject + verb (base form)
- The function returns data. → Fungsi mengembalikan data.
- The API accepts POST requests. → API menerima permintaan POST.

Avoid past tense in README: → "It returned" → "It returns"
